# Core Garden + API (backend container)
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0

# Training (local, not needed in backend container)
numpy==1.24.3
pandas==2.0.3
torch==2.1.0
tqdm==4.66.1
sentencepiece==0.1.99
matplotlib==3.7.2
mlflow>=2.9.0

# NLP (optional, for advanced tokenization)
spacy==3.7.2
spacy-transformers==1.3.4
